# Test profile — uses the same datasource as dev (from environment variables).
# Controller tests mock all services, so no real DB is needed for those.
# Repository tests use Replace.NONE and require DB_URL/DB_USERNAME/DB_PASSWORD in env.
spring.flyway.enabled=false
spring.jpa.hibernate.ddl-auto=none
